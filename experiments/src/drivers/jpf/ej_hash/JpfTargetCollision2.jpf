@include ../common-jpf.properties

# Set application classpath via the command line: '+classpath=...'

target = JpfTargetCollision2
symbolic.method = EffectiveJavaHashCode.testCollision2(sym#sym#sym#sym)
